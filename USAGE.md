# Gones 使用说明

## 快速开始

1. **编译程序**：
   ```bash
   go build -o gones .
   ```

2. **运行模拟器**：
   ```bash
   ./gones
   ```

3. **选择 ROM 文件**：
   - 程序启动后会弹出文件选择对话框
   - 选择 .nes 格式的游戏文件
   - 支持 Mapper 0 的游戏（如 Super Mario Bros.）

## 控制说明

### 键盘控制
- **Z**: A 按钮
- **X**: B 按钮  
- **A**: Select 按钮
- **S**: Start 按钮
- **方向键**: 控制方向
- **ESC**: 切换菜单

### 手柄控制
- **按钮 0**: A 按钮
- **按钮 1**: B 按钮
- **按钮 6**: Select 按钮
- **按钮 7**: Start 按钮
- **D-pad**: 方向控制

## 故障排除

### 常见问题

1. **程序无法启动**
   - 确保已安装 Go 1.24.3 或更高版本
   - 检查依赖是否正确安装：`go mod tidy`

2. **ROM 无法加载**
   - 确保 ROM 文件是有效的 .nes 格式
   - 检查 ROM 是否使用 Mapper 0

3. **画面显示异常**
   - 这是正常现象，模拟器仍在开发中
   - 某些游戏可能显示测试图案

### 调试信息

程序运行时会输出调试信息，包括：
- ROM 加载状态
- PPU 渲染信息
- 帧计数

## 开发模式

如果需要修改代码：
1. 编辑源代码
2. 重新编译：`go build -o gones .`
3. 运行：`./gones`

## 支持的游戏类型

- **Mapper 0**: 最基本的 NES 游戏
- **文件大小**: 通常 16KB-64KB PRG + 8KB CHR
- **示例游戏**: Super Mario Bros., Duck Hunt

## 性能说明

- 目标帧率：60 FPS
- 窗口大小：512x480 (2x 缩放)
- 支持全屏模式（通过 Ebiten 设置）
